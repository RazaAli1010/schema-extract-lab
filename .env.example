# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Anthropic API key for the teacher labeling pipeline (F2).
ANTHROPIC_API_KEY=

# Hugging Face token — needed only to push the LoRA adapter to the Hub (F6).
# Not needed for Qwen3-1.7B itself, which is ungated.
HF_TOKEN=
